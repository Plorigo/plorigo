# CLA signature storage

This branch is managed automatically by the [CLA Assistant](https://github.com/contributor-assistant/github-action) workflow (`.github/workflows/cla.yml`).

Contributor signatures are stored in `signatures/version1/cla.json`. Do **not** delete this branch or enable branch protection on it — the action commits signatures here directly.
